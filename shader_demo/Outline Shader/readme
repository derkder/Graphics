后处理
第一种：深度值
  为了生成轮廓，我们将对相邻的像素进行采样，并比较其数值。如果数值相差很大，我们就会画出一个边缘。
  我们将以X形从深度缓冲区中取样像素，大致以当前正在渲染的像素为中心。
第二种：法线
  附在相机上的是一个叫做RenderReplacementShaderToTexture的脚本，设置为生成一个相机，将场景的视图空间法线渲染到_CameraNormalsTexture。我们将再次采取这些样本之间的差异来检测轮廓。
第三种：边缘检测算子
  https://zhuanlan.zhihu.com/p/555969528
